# CryptoNews Agent 🤖 v15.5 (Auto-Pilot)

Автономная AI-редакция для Telegram. Бот находит новости, переписывает их, генерирует уникальные изображения и управляет публикацией через удобный пульт.

## 🚀 Что нового в v15.5:

### 🧠 Auto-AI Brain (Новинка!)
* **Умный выбор модели:** Бот сам тестирует доступные модели Gemini (2.0 Flash Exp, 1.5 Flash) при запуске и выбирает самую быструю и бесплатную.
* **Anti-Flood:** Если Google ограничивает запросы (429), бот не падает, а ждет или меняет стратегию.
* **Smart Fallback:** Если нейросеть недоступна, новость не теряется, а обрабатывается по резервному сценарию.

### 🎨 Image Engine v13.1
* **Smart Retries:** Если генератор картинок (Pollinations) сбоит, бот делает **3 попытки** скачивания с паузами.
* **Style Control:** Генерация 4 вариантов: Киберпанк, Арт, Оригинал (из новости) и Ремейк (AI-переосмысление).

### 🛡 Стабильность и UI
* **Self-Cleaning:** Интерфейс бота (кнопки, превью) автоматически удаляется после публикации, не засоряя канал модерации.
* **Safe-Delete:** Умная система удаления сообщений, которая не вызывает ошибок Telegram API.

## 🛠 Требования
* Python 3.12+
* Google Gemini API Key (бесплатно)
* Telegram API ID & Hash

## ⚡️ Запуск
1. Установите зависимости: `pip install -r requirements.txt`
2. Настройте `.env`
3. Запустите: `start_bot.bat` (или `python run.py`)
